# Task 8: Frontend — FrequencyEditor + PredictionsTab

**Files:**
- Create: `apps/web/src/components/attendance/FrequencyEditor.tsx`
- Create: `apps/web/src/components/attendance/PredictionsTab.tsx`

**Interfaces:**
- Consumes: `attendanceAPI.predict()`
- Produces: `FrequencyEditor` component, `PredictionsTab` component

## Steps

1. Create FrequencyEditor component (see plan for complete code)
2. Create PredictionsTab component (see plan for complete code)
3. Commit: `git add apps/web/src/components/attendance/FrequencyEditor.tsx apps/web/src/components/attendance/PredictionsTab.tsx && git commit -m "feat: add FrequencyEditor and PredictionsTab components"`

**Working directory:** `D:\Alpha Coders\CampusFlow`
