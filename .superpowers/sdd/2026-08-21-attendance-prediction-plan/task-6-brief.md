# Task 6: Frontend — UploadTab Component

**Files:**
- Create: `apps/web/src/components/attendance/UploadTab.tsx`

**Interfaces:**
- Consumes: `attendanceAPI.parse()`, `attendanceAPI.save()`
- Produces: `UploadTab` React component

## Steps

1. Install Tesseract.js: `cd apps/web && npm install tesseract.js`
2. Create `apps/web/src/components/attendance/UploadTab.tsx` with full implementation (see plan for complete code)
3. Test in browser
4. Commit: `git add apps/web/src/components/attendance/UploadTab.tsx apps/web/package.json apps/web/package-lock.json && git commit -m "feat: add UploadTab with Tesseract.js OCR and AI parsing"`

**Working directory:** `D:\Alpha Coders\CampusFlow`
