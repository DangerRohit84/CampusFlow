# Task 10: Frontend — Rewrite AttendancePage with Tabs

**Files:**
- Modify: `apps/web/src/pages/AttendancePage.tsx`

**Interfaces:**
- Consumes: All 4 tab components from Tasks 6-9
- Produces: Updated AttendancePage with tab navigation

## Steps

1. Rewrite `apps/web/src/pages/AttendancePage.tsx` with 5-tab layout (see plan for complete code)
2. Test all tabs in browser
3. Test dark mode
4. Commit: `git add apps/web/src/pages/AttendancePage.tsx && git commit -m "feat: rewrite AttendancePage with 5-tab layout"`

**Working directory:** `D:\Alpha Coders\CampusFlow`
