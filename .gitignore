node_modules/
.turbo/
*.log
.env
.env*
!.env.example
!.env.local.example
dist/
build/
.DS_Store
*.pdf
*.db
*.db-journal
backend.err.log
frontend.err.log
uploads/
*.tsbuildinfo
*.tmp
coverage/

# Local backup files (never track *.bak)
*.bak
*.bak*
*.env.bak*
.env.*.bak*
# DB dumps / snapshots (never track — contain PII + creds)
neon_dump*
sg18_dump*
*dump*.bak
dump*.sql

# Temp verification scripts (cleanup 2026-09-05)
packages/backend/test-sync*
packages/backend/verify-*.js
tools/run-resume*
tools/qa-*.js

# Runtime / tool caches (never track)
.vitest/
.expo/
.next/
playwright-report/
test-results/

# AI agent reports (never track — local only, history preserved)
.ai/reports/
packages/backend/.ai/reports/

# Duplicate brand assets (canonical copy is apps/web/public/brand/, test-only reference)
apps/web/src/assets/brand/
# Brand source files (Illustrator/.ai/.eps + guides, local only)
campusflow-brand-kit/

