# CommandPalette.tsx

## Overview
Keyboard-driven command palette (Ctrl+K) for quick navigation.

## Changes

### 2026-08-06 — Initial Build
- Created with command search and navigation
- Added optional `open`/`onClose` props for external control

## Dependencies
- `src/store/appStore.ts` — App state
